version: '3.8'

services:
  wavefront-api:
    build: .
    container_name: wavefront-youtube-api
    ports:
      - "8000:8000"
    environment:
      - DOWNLOAD_DIR=/tmp/wavefront_downloads
      - MAX_CONCURRENT_DOWNLOADS=3
      - CLEANUP_AFTER_HOURS=24
    volumes:
      - downloads:/tmp/wavefront_downloads
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

volumes:
  downloads:
