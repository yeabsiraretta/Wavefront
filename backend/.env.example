# Wavefront YouTube API Configuration

# Directory for downloaded files
DOWNLOAD_DIR=/tmp/wavefront_downloads

# Maximum concurrent downloads
MAX_CONCURRENT_DOWNLOADS=3

# Hours after which completed downloads are cleaned up
CLEANUP_AFTER_HOURS=24

# Server configuration (for production)
# HOST=0.0.0.0
# PORT=8000
